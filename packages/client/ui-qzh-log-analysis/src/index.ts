/**
 * QZH log-analysis workbench node half. Host-side case APIs and the read-only
 * code evidence service are added in later phases; the browser half is loaded
 * through the dsh.client manifest.
 */

/** Host plugin body for the P0 browser workbench. */
export function apply(): void {}
